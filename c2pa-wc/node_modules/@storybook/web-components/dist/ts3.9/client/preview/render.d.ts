import { RenderContext } from '@storybook/store';
import { WebComponentsFramework } from './types-6-0';
export declare function renderToDOM({ storyFn, kind, name, showMain, showError, forceRemount }: RenderContext<WebComponentsFramework>, domElement: HTMLElement): void;
